<div class="email-header">From:</div>
 <%= @entry.source.name %> ( <%= @entry.source.address %> )
<div class="email-header">Subject:</div>
<%= @entry.email_content.subject %>
<div class="email-header">Body:</div>
 <%= raw  @entry.email_content.body %>
